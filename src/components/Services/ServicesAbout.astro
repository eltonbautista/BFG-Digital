---
import Button, { ButtonTwo } from "../Button";
const { content } = Astro.props;
---

<section class="max-w-full bg-neutral text-primary-content pb-10 lg:pb-32">
  <div
    class="relative mx-auto flex flex-col px-4 md:px-6 lg:px-8 sm:max-w-xl md:max-w-screen-xl md:flex-row"
  >
    <!-- Left Column -->
    <div
      class="my-auto mx-auto mt-10 w-full max-w-xl md:mt-56 lg:max-w-screen-xl"
    >
      <div class="mb-16 lg:mb-0 lg:max-w-xl">
        <div class="mb-6 max-w-2xl">
          <div>
            <p
              class="bg-teal-accent-400 mb-2 inline-block rounded-full py-px text-xs font-semibold uppercase tracking-wider"
            >
              {content.aboutMiniHeading}
            </p>
          </div>
          <h2
            class="mb-6 max-w-lg text-3xl font-bold tracking-tight sm:text-5xl sm:leading-snug"
          >
            {content.aboutHeading}
          </h2>
          <p class="">
            {content.aboutDescription}
          </p>
        </div>
        <div class="flex items-center gap-x-4">
          <Button title="Start Now" link="/" />
          <ButtonTwo link="#serviceSteps">Our Process</ButtonTwo>
        </div>
      </div>
    </div>
    <!-- /Left Column -->

    <!-- Right Column -->
    <div
      class="grid md:flex h-full w-full md:space-x-3 overflow-hidden md:justify-end lg:px-2"
    >
      <!-- Col 2 -->
      <div
        class="my-auto mb-20 hidden w-56 flex-col space-y-3 md:mt-36 lg:flex"
      >
        <div class="md:rounded-xl bg-info-content py-5">
          <img
            class="h-full w-full object-contain"
            src={content.aboutImageOne}
            alt={content.aboutAltOne}
            width="300px"
            height="400px"
          />
        </div>
      </div>
      <div
        class="my-auto flex md:rounded-xl md:mt-36 md:w-60 md:flex-col md:space-y-3 space-x-0 md:px-4"
      >
        <div class="h-40 overflow-hidden md:rounded-xl bg-success">
          <img
            class="mx-auto h-full w-full object-bottom object-cover md:object-contain"
            src={content.aboutImageTwo}
            alt={content.aboutAltTwo}
            width="300px"
            height="400px"
          />
        </div>
        <div class="h-40 overflow-hidden md:rounded-xl bg-error">
          <img
            class="mx-auto h-full w-full object-cover object-bottom md:object-contain"
            src={content.aboutImageThree}
            alt={content.aboutAltThree}
            width="300px"
            height="400px"
          />
        </div>
        <div
          class="h-40 overflow-hidden md:rounded-xl bg-warning-content hidden md:block"
        >
          <img
            class="mx-auto h-full w-full object-cover object-bottom md:object-contain"
            src={content.aboutImageFour}
            alt={content.aboutAltFour}
            width="300px"
            height="400px"
          />
        </div>
      </div>
    </div>
    <!-- /Right Column -->
  </div>
</section>
